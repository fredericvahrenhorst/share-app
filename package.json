{
    "name": "share-app",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "start:payload": "cd ./apps/payload && npm start",
        "build": "turbo run build",
        "generate": "turbo run generate",
        "dev": "turbo run dev"
    },
    "devDependencies": {
        "turbo": "^2.0.9"
    },
    "engines": {
        "node": ">=20.9.0"
    },
    "packageManager": "pnpm@9.5.0"
}
